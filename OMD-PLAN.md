# OMD-PLAN — kfcman.link 전체 리스킨

- run_id: `run-2026-08-10T19-46-27-012Z-kfcman-link-전체-리스킨-drdiary-기반`
- created_at: 2026-08-11

## Slots

| Slot | Value |
|---|---|
| intent | kfcman.link 기존 화면 전체(랜딩 히어로, 하이라이트 카드, 최근 프로젝트 그리드, 통합 설정/관리자 패널, 모달)를 DESIGN.md 토큰으로 리스킨. **새 화면 설계 아님** — 기존 index.html/app.js 구조·기능은 그대로 유지. |
| audience | 초등교사(주 사용자 — 링크 생성/학급 관리), 학생(참여형 도구 사용자 — 설문/칸반/테트리스), 학교 관리자(전교 계정 관제) — DESIGN.md §13 페르소나와 동일 |
| tone_seed | `drdiary` (Dr.diary 헬스케어 브랜드 레퍼런스) — 이미 DESIGN.md로 hybrid 변형 완료 |
| exit_scope | **direct-apply** — 이 프로젝트는 이미 운영 중인 라이브 서비스라 wireframe/handoff-zip이 아니라 실제 코드(HTML/CSS/Tailwind 클래스)에 토큰을 직접 적용. 로컬 검증 → 승인 → 프로덕션 배포까지 진행. |

## 적용 토큰 (DESIGN.md 그대로, 커스터마이즈 없음)

- Primary: 스카이블루 `#3eaeff` / 그라데이션 `#3eaeff → #ff5a8c → #dc6eff` (+`#00c8fa` 시안 꼬리)
- Ink: `#232f4d` (헤딩) / `#000000` (본문)
- Surface: `#ffffff` (캔버스) / `#f5f8fb` (톤 배경) / `#dee0e4` (헤어라인)
- Font: Pretendard 전체 (헤드라인 40px/600, 카드타이틀 18px/500, 본문 16px/400)
- Radius: 8px(CTA) / 16px(카드) / pill·9999px(뱃지)
- Shadow: 없음(flat) — 구분은 톤 배경 + 헤어라인으로만

## 적용 범위 (기존 구조 유지, 스타일만 교체)

1. **랜딩 히어로** — 헤드라인 "학교의 모든 연결을, 더 가볍고 빠르게." 그라데이션 클립 텍스트 적용, CTA 버튼을 outline 스타일(8px radius)로
2. **하이라이트 카드 3종** ("짧고 명확한 연결" 등) — `#f5f8fb` 톤 배경 카드, 16px radius, 그림자 제거
3. **최근 프로젝트 그리드 카드** — 흰 배경 + `#dee0e4` 헤어라인 + 16px radius + 그림자 제거 (현재 shadow-sm/hover:shadow-md 클래스 제거)
4. **통합 설정/관리자 패널** (이번 세션에 추가한 "전체 회원 단축 링크 관리" 포함) — 카드/테이블을 flat 스타일로 통일
5. **모달** (수정/QR/통계) — 그림자 제거, 헤어라인 테두리로 전환
6. **폰트** — 전역 Pretendard 적용 확인 (이미 로드돼 있는지 head 확인 필요)

## 적용 안 하는 것

- 클라이언트 role별 접근 제어, API 로직, DB 스키마 — 손대지 않음
- 기능 흐름(어떤 버튼이 어떤 동작을 하는지) — 변경 없음
- www/ 미러 폴더는 프로덕션에서 실제로 서빙되지 않으므로(이전 조사 확인됨) 로컬 저장소 동기화만, 배포 대상 아님

## 진행 순서

1. 로컬에서 index.html/style.css의 색상·폰트·카드 클래스를 토큰 기준으로 교체
2. 로컬 서버로 브라우저 확인 (라이트/다크 모드 각각)
3. 스크린샷으로 전/후 비교 제시 → 승인
4. 승인 시 프로덕션 백업 → 패치 적용 → 캐시 버전 갱신 → pm2 재시작 → 검증 (기존 세션에서 쓴 방식과 동일)
